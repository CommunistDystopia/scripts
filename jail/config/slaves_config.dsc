Slaves_Config:
    type: data
    # Slave time (minutes) by default when added as slave (by command or killed by a Soldier)
    slave_timer: 120
    # Slave time (minutes) added by a jailstick
    slave_timer_jailstick: 120
    # Slave time (minutes) added by failing in a Court
    court_fail_timer_add: 120
